\tikzset{every picture/.style={execute at begin picture={
   \shorthandoff{:;!?};}
}}

\tikzset{
    boxnode/.style={ % requires library shapes.misc
        draw,
        rectangle,
        text centered,
        align=center,
        fill=gray!5!white
    },
}